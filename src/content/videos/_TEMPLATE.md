---
# =============================================================================
# RETRO POST TEMPLATE  —  copy this file, rename it, fill it in.
# =============================================================================
# HOW TO USE:
#   1. Copy this file in the same folder (src/content/retro/).
#   2. Rename the copy to a short url-friendly name, e.g. tecmo-week-04.md
#      (lowercase, dashes, no spaces — this becomes the page address).
#   3. Fill in every field below, then write your post under the second ---.
#   4. Delete these comment lines if you like (the # lines). Optional.
#   5. Save, commit, push. It appears on the site automatically.
#
# The file name MUST NOT start with an underscore _ (that hides it).
# Leave THIS template file named _TEMPLATE.md so it never publishes.
# =============================================================================

title: "GAME — Team Week N vs Opponent"   # the headline. Keep it punchy.
deck: "One line that sells the episode."   # optional subtitle. Delete if unused.
publishDate: 2026-01-01                     # YYYY-MM-DD. The day you post it.
episodeNumber: 1                            # which episode in the series.
game: "Tecmo Super Bowl"                    # the game being played.
series: "Seahawks Season 1"                 # optional. The run this belongs to.
youtubeId: "REPLACE_ME"                     # the YouTube video ID (see guide).
featured: false                            # true = pin to top of the Arcade.
readMinutes: 3                              # optional. Rough read time.
draft: true                                # true = hidden. Set false to publish.
---

Write your post here, below the second set of dashes.

This is the part that is all you — what happened in the game, what was fun
or brutal about it, what to watch for in the video. A short paragraph or two
is plenty. Your voice is the point.

When you're ready for the world to see it, change `draft: true` to
`draft: false` above, then commit and push.
