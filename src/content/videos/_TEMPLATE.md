---
# =============================================================================
# VIDEO TEMPLATE  —  copy this file, rename it, fill it in.
# =============================================================================
# HOW TO USE:
#   1. Copy this file in the same folder (src/content/videos/).
#   2. Rename the copy to a short name, e.g. tecmo-week-04.md
#      (lowercase, dashes, no spaces — this is just an internal key).
#   3. Fill in the fields below. Only title, publishDate, and youtubeId are
#      required; the rest are optional.
#   4. Save, commit, push. The newest video becomes the front-page lead
#      automatically; older ones stack below it.
#
# The file name MUST NOT start with an underscore _ (that hides it).
# Leave THIS template file named _TEMPLATE.md so it never publishes.
# =============================================================================

title: "Team Week N vs Opponent"           # the headline. Keep it punchy.
deck: "One line that sells the episode."   # optional blurb on the card.
publishDate: 2026-01-01                     # YYYY-MM-DD. The day you post it.
youtubeId: "REPLACE_ME"                     # the YouTube video ID (required).
episodeNumber: 1                            # optional. Which episode in the run.
game: "Tecmo Super Bowl"                    # optional. The game being played.
series: "Seahawks Season 1"                 # optional. The run this belongs to.
customImage: ""                            # optional. A URL to a game screenshot
                                            # to use instead of the YouTube
                                            # thumbnail. Leave blank to default
                                            # to the thumbnail. Delete if unused.
featured: false                            # reserved flag. Optional.
draft: true                                # true = hidden. Set false to publish.
---

Notes to self go here (optional). Nothing below the dashes is shown on the
site — every video links straight to YouTube — so this space is just a
scratchpad for you.
