---
# =============================================================================
# COLUMN / ARTICLE TEMPLATE  —  copy this file, rename it, fill it in.
# =============================================================================
# HOW TO USE:
#   1. Copy this file in the same folder (src/content/articles/).
#   2. Rename the copy to a short url-friendly name, e.g. mariners-rebuild.md
#      (lowercase, dashes, no spaces — this becomes the page address).
#   3. Fill in the fields, then write your column under the second ---.
#   4. Save, commit, push. It appears on the site automatically and shows up
#      on the home page, the Columns archive, the team page, your author page,
#      the ticker, and the RSS feed. One file, everywhere.
#
# Leave THIS template named _TEMPLATE.md so it never publishes.
# =============================================================================

title: "Your Headline Here"                 # the headline.
deck: "A sentence that sets up the piece."  # optional subtitle. Delete if unused.
publishDate: 2026-01-01                      # YYYY-MM-DD.
tag: "Column"                            # pick ONE (see list below).
team: "general"                             # seahawks | mariners | kraken | sonics | general
lead: false                                # true = the big lead story on the home page.
featured: false                            # true = highlight in lists.
readMinutes: 8                              # optional. Rough read time.
draft: true                                # true = hidden. Set false to publish.
---

# Allowed tag values (copy one exactly into `tag` above):
# Column | Almanac
# Seahawks | Mariners | Kraken | Sonics | General
# Only ONE post should have `lead: true` at a time — that's the front-page
# headliner. The rest are false.

Write your column here, below the second set of dashes.

This is yours. The byline (Two Oh Six) is added automatically from your
site config, so you don't type it. When ready, set `draft: false`, commit,
and push.
